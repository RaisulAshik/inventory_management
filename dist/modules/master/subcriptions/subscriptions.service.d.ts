import { Repository } from 'typeorm';
import { Subscription } from '@entities/master/subscription.entity';
import { SubscriptionPlan } from '@entities/master/subscription-plan.entity';
import { BillingHistory } from '@entities/master/billing-history.entity';
import { Tenant } from '@entities/master/tenant.entity';
import { PaginationDto } from '@common/dto/pagination.dto';
import { PaginatedResult } from '@common/interfaces';
import { BillingCycle } from '@common/enums';
import { CreateSubscriptionDto } from './dto/create-subscription.dto';
import { UpdateSubscriptionDto } from './dto/update-subscription.dto';
export declare class SubscriptionsService {
    private readonly subscriptionRepository;
    private readonly planRepository;
    private readonly billingHistoryRepository;
    private readonly tenantRepository;
    constructor(subscriptionRepository: Repository<Subscription>, planRepository: Repository<SubscriptionPlan>, billingHistoryRepository: Repository<BillingHistory>, tenantRepository: Repository<Tenant>);
    create(createDto: CreateSubscriptionDto): Promise<Subscription>;
    private calculatePeriodEnd;
    findAll(paginationDto: PaginationDto): Promise<PaginatedResult<Subscription>>;
    findById(id: string): Promise<Subscription>;
    findByTenantId(tenantId: string): Promise<Subscription | null>;
    update(id: string, updateDto: UpdateSubscriptionDto): Promise<Subscription>;
    changePlan(id: string, newPlanId: string, billingCycle?: BillingCycle): Promise<Subscription>;
    cancel(id: string, cancelImmediately?: boolean): Promise<Subscription>;
    reactivate(id: string): Promise<Subscription>;
    renew(id: string): Promise<Subscription>;
    private createBillingRecord;
    getBillingHistory(tenantId: string): Promise<BillingHistory[]>;
    processExpiringSubscriptions(): Promise<void>;
    processTrialExpirations(): Promise<void>;
}
