import { TenantConnectionManager } from '@database/tenant-connection.manager';
import { PaginationDto } from '@common/dto/pagination.dto';
import { PaginatedResult } from '@common/interfaces';
import { Customer, CustomerAddress, CustomerDue } from '@entities/tenant';
import { CreateCustomerAddressDto } from './dto/create-customer-address.dto';
import { CreateCustomerDto } from './dto/create-customer.dto';
import { CustomerFilterDto } from './dto/customer-filter.dto';
import { UpdateCustomerDto } from './dto/update-customer.dto';
export declare class CustomersService {
    private readonly tenantConnectionManager;
    constructor(tenantConnectionManager: TenantConnectionManager);
    private getCustomerRepository;
    create(createCustomerDto: CreateCustomerDto, createdBy: string): Promise<Customer>;
    private createAddresses;
    private createCredentials;
    findAll(filterDto: CustomerFilterDto): Promise<PaginatedResult<Customer>>;
    findById(id: string): Promise<Customer>;
    findByEmail(email: string): Promise<Customer | null>;
    findByCode(code: string): Promise<Customer | null>;
    update(id: string, updateCustomerDto: UpdateCustomerDto): Promise<Customer>;
    remove(id: string): Promise<void>;
    addAddress(customerId: string, addressDto: CreateCustomerAddressDto): Promise<CustomerAddress>;
    updateAddress(addressId: string, addressDto: Partial<CreateCustomerAddressDto>): Promise<CustomerAddress>;
    removeAddress(addressId: string): Promise<void>;
    getAddresses(customerId: string): Promise<CustomerAddress[]>;
    getOutstandingBalance(customerId: string): Promise<number>;
    getDues(customerId: string): Promise<CustomerDue[]>;
    getOrderHistory(customerId: string, paginationDto: PaginationDto): Promise<PaginatedResult<any>>;
    updateCreditLimit(customerId: string, creditLimit: number): Promise<Customer>;
    checkCreditLimit(customerId: string, orderAmount: number): Promise<boolean>;
    count(): Promise<number>;
}
